# Minimal requirements for CPU-only deployment
# Use this if you encounter issues with GPU-specific packages

# Core dependencies
torch>=2.0.0
transformers>=4.30.0
openai-whisper>=20230314

# Audio processing (sounddevice optional for HF Spaces)
numpy>=1.24.0
scipy>=1.10.0

# Web interface
gradio>=4.0.0
pandas>=2.0.0

# API and utilities
requests>=2.28.0
ffmpeg-python>=0.2.0

# Note: Excluded packages that may cause issues on CPU:
# - sounddevice (desktop audio capture)
# - unsloth (GPU optimization)
# - bitsandbytes (GPU quantization)